name: "Deploy Codigo Facilito"

on:
  push:
    branches:
      - main

jobs:
  deploy-codigofacilito:
    runs-on: 
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploying Codigo Facilito..."
      - run: echo "Deployment completed successfully!"